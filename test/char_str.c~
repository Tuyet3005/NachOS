#include "syscall.h" 
#include "copyright.h" 
#define maxlen 32 
int main() 
{ 
    char i, buffer[maxlen];
    PrintChar('a');

    PrintString("xyz");
    PrintChar('\n');

        i= ReadChar();
    PrintChar(i);
    PrintChar('\n');

    ReadString(buffer, maxlen);
    PrintString(buffer);

    Halt();
}

    
