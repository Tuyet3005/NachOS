#include "syscall.h" 
#include "copyright.h"
 
#define maxlen 255
int main() 
{ 
    char buffer[maxlen];
    int result;
    result = ReadInt(buffer, maxlen);
    PrintString("toi day 1\n");
    PrintInt(result);
    PrintString("toi day 2\n");
    Halt();
}

    
